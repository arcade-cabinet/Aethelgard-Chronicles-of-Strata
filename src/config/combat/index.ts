/**
 * config/combat — the combat domain bundle (M_V13.DECOMP.CONFIG-COMBAT).
 *
 * Combat tuning (combat.json + accessor: unit stats, spawn intervals,
 * difficulty multipliers, AI vision) and the unit/building archetypes
 * (archetypes.json + accessor: mesh ids, sfx, particle palettes).
 */
export * from './combat';
export * from './archetypes';
