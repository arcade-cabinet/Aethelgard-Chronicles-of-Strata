/**
 * config/assets — the asset-metadata domain bundle
 * (M_V13.DECOMP.CONFIG-ASSETS).
 *
 * Per-asset tuning (asset-metadata.json + accessor): the measured
 * bounding-box scales + rig metadata the loaders apply to GLBs so
 * models sit correctly on the hex grid.
 */
export * from './asset-metadata';
